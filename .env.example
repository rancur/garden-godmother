# Optional — integrations can be configured via Settings > Integrations after deployment
OPENAI_API_KEY=
HA_TOKEN=
HA_URL=http://homeassistant.local:8123

# Production deployment — set these for your domain
# CORS_ORIGINS=https://garden.yourdomain.com,http://localhost:3400
# COOKIE_DOMAIN=.yourdomain.com
