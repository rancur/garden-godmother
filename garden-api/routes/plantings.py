"""Planting list endpoint (main CRUD is in beds.py)."""
from __future__ import annotations

from fastapi import APIRouter

# The /api/plantings endpoint is in routes/beds.py alongside other planting CRUD.
# This module exists as a placeholder for future planting-specific endpoints.

router = APIRouter()
